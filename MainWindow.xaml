<Window
    x:Class="Poe2BuildManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="PoE2 Build Manager"
    Height="800"
    Width="1400"
    MinHeight="700"
    MinWidth="1100">

    <Window.Resources>

        <Style TargetType="GridViewColumnHeader">
            <Setter Property="HorizontalContentAlignment"
                    Value="Left" />
            <Setter Property="FontWeight"
                    Value="SemiBold" />
        </Style>

    </Window.Resources>

    <Grid Margin="12">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->

        <StackPanel Grid.Row="0">

            <TextBlock
                FontSize="24"
                FontWeight="SemiBold"
                Text="PoE2 Build Manager" />

            <TextBlock
                Margin="0,4,0,0"
                Foreground="Gray"
                Text="Drag .build or .zip files directly into the build list." />

        </StackPanel>

        <!-- Build Folder -->

        <GroupBox
            Grid.Row="1"
            Margin="0,15,0,15"
            Header="Build Folder">

            <Grid Margin="8">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox
                    Grid.Column="0"
                    Margin="0,0,10,0"
                    Text="{Binding BuildFolder}"
                    IsReadOnly="True"
                    VerticalContentAlignment="Center" />

                <Button
                    Grid.Column="1"
                    Width="100"
                    Margin="0,0,10,0"
                    Content="Browse..."
                    Click="BrowseBuildFolder_Click" />

                <Button
                    Grid.Column="2"
                    Width="100"
                    Content="Open Folder"
                    Click="OpenFolder_Click" />

            </Grid>

        </GroupBox>

        <!-- Main Area -->

        <Grid Grid.Row="2">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <!-- Left Side -->

            <Grid Grid.Column="0">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <CheckBox
                    Grid.Row="0"
                    Margin="8,0,0,8"
                    Content="Hide disabled builds"
                    IsChecked="{Binding HideDisabledBuilds}" />

                <GroupBox
                    Grid.Row="1"
                    Header="{Binding BuildListHeader}">

                    <ListView
                        Margin="8"
                        ItemsSource="{Binding BuildPlans}"
                        SelectedItem="{Binding SelectedBuildPlan}"
                        AllowDrop="True"
                        DragEnter="BuildPlans_DragEnter"
                        Drop="BuildPlans_Drop">

                        <ListView.ItemContainerStyle>

                            <Style TargetType="ListViewItem">

                                <Setter Property="Padding"
                                        Value="4"/>

                                <Style.Triggers>

                                    <DataTrigger
                                        Binding="{Binding IsDisabled}"
                                        Value="True">

                                        <Setter Property="Foreground"
                                                Value="Gray"/>

                                    </DataTrigger>

                                </Style.Triggers>

                            </Style>

                        </ListView.ItemContainerStyle>

                        <ListView.View>

                            <GridView>

                                <GridViewColumn
                                    Header="Build Name"
                                    Width="450"
                                    DisplayMemberBinding="{Binding BuildName}" />

                                <GridViewColumn
                                    Header="Ascendancy"
                                    Width="120"
                                    DisplayMemberBinding="{Binding Ascendancy}" />

                                <GridViewColumn
                                    Header="Description"
                                    Width="250"
                                    DisplayMemberBinding="{Binding Description}" />

                            </GridView>

                        </ListView.View>

                    </ListView>

                </GroupBox>

            </Grid>

            <!-- Details Panel -->

            <GroupBox
                Grid.Column="1"
                Margin="10,0,0,0"
                Header="Build Details">

                <Grid>

                    <!-- Empty State -->

                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontSize="16"
                        Foreground="Gray"
                        Text="Select a build to view details">

                        <TextBlock.Style>

                            <Style TargetType="TextBlock">

                                <Setter Property="Visibility"
                                        Value="Collapsed"/>

                                <Style.Triggers>

                                    <DataTrigger
                                        Binding="{Binding SelectedBuildPlan}"
                                        Value="{x:Null}">

                                        <Setter Property="Visibility"
                                                Value="Visible"/>

                                    </DataTrigger>

                                </Style.Triggers>

                            </Style>

                        </TextBlock.Style>

                    </TextBlock>

                    <ScrollViewer VerticalScrollBarVisibility="Auto">

                        <ScrollViewer.Style>

                            <Style TargetType="ScrollViewer">

                                <Setter Property="Visibility"
                                        Value="Visible"/>

                                <Style.Triggers>

                                    <DataTrigger
                                        Binding="{Binding SelectedBuildPlan}"
                                        Value="{x:Null}">

                                        <Setter Property="Visibility"
                                                Value="Collapsed"/>

                                    </DataTrigger>

                                </Style.Triggers>

                            </Style>

                        </ScrollViewer.Style>

                        <Grid Margin="12">

                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>

                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>

                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>

                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>

                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>

                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock
                                Grid.Row="0"
                                FontWeight="SemiBold"
                                Text="File Name" />

                            <TextBox
                                Grid.Row="1"
                                Margin="0,5,0,10"
                                IsReadOnly="True"
                                Background="#F5F5F5"
                                BorderBrush="#D0D0D0"
                                Text="{Binding SelectedBuildPlan.FileName}" />

                            <TextBlock
                                Grid.Row="2"
                                FontWeight="SemiBold"
                                Text="Build Name" />

                            <TextBox
                                Grid.Row="3"
                                Margin="0,5,0,10"
                                Text="{Binding SelectedBuildPlan.BuildName}" />

                            <TextBlock
                                Grid.Row="4"
                                FontWeight="SemiBold"
                                Text="Author" />

                            <TextBox
                                Grid.Row="5"
                                Margin="0,5,0,10"
                                Text="{Binding SelectedBuildPlan.Author}" />

                            <TextBlock
                                Grid.Row="6"
                                FontWeight="SemiBold"
                                Text="Ascendancy" />

                            <TextBox
                                Grid.Row="7"
                                Margin="0,5,0,10"
                                Text="{Binding SelectedBuildPlan.Ascendancy}" />

                            <TextBlock
                                Grid.Row="8"
                                FontWeight="SemiBold"
                                Text="Description" />

                            <TextBox
                                Grid.Row="9"
                                Height="250"
                                Margin="0,5,0,10"
                                AcceptsReturn="True"
                                TextWrapping="Wrap"
                                VerticalScrollBarVisibility="Auto"
                                Text="{Binding SelectedBuildPlan.Description}" />

                            <StackPanel
                                Grid.Row="10"
                                Orientation="Horizontal">

                                <Button
                                    Width="130"
                                    Height="32"
                                    Content="Save Changes"
                                    Command="{Binding SaveBuildCommand}" />

                                <Button
                                    Width="130"
                                    Height="32"
                                    Margin="10,0,0,0"
                                    Content="Delete Build"
                                    Command="{Binding DeleteBuildPlanCommand}" />

                                <Button
                                    Width="130"
                                    Height="32"
                                    Margin="10,0,0,0"
                                    Content="{Binding EnableDisableButtonText}"
                                    Command="{Binding ToggleBuildEnabledCommand}" />

                                <Button
                                    Width="120"
                                    Margin="10,0,0,0"
                                    Content="View JSON"
                                    Command="{Binding ViewJsonCommand}" />

                            </StackPanel>

                        </Grid>

                    </ScrollViewer>

                </Grid>

            </GroupBox>

        </Grid>

    </Grid>

</Window>